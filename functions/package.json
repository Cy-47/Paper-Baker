{
  "name": "@paper-baker/functions",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup && (tsup --watch & TSUP_PID=$!; trap 'kill $TSUP_PID 2>/dev/null' EXIT; PATH=\"/opt/homebrew/opt/openjdk/bin:$PATH\" firebase emulators:start --only auth,firestore,functions --project paper-baker)",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "latest",
    "firebase-functions": "latest"
  },
  "devDependencies": {
    "@types/express": "^4",
    "tsup": "latest",
    "typescript": "latest"
  }
}
